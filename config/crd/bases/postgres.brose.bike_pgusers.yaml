---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: pgusers.postgres.brose.bike
spec:
  group: postgres.brose.bike
  names:
    kind: PgUser
    listKind: PgUserList
    plural: pgusers
    singular: pguser
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: PgUser is the Schema for the pgusers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: PgUserSpec defines the desired state of PgUser
            properties:
              databases:
                description: Databases is an example field of PgLoginRole
                items:
                  description: TODO
                  properties:
                    name:
                      description: TODO
                      type: string
                    owner:
                      description: TODO
                      type: boolean
                    privileges:
                      description: TODO
                      items:
                        type: string
                      type: array
                  required:
                  - privileges
                  type: object
                type: array
              instance:
                description: Instance identifies the PgInstanceConnection which should
                  be used
                properties:
                  name:
                    description: Name identifies the PgInstanceConnection which should
                      be used
                    type: string
                  namespace:
                    description: Namespace defines the namespace in which the PgInstanceConnection
                      is located
                    type: string
                required:
                - name
                - namespace
                type: object
              secret:
                description: Secret is an example field of PgLoginRole
                properties:
                  name:
                    description: Name identifies the PgLoginRoleSecret which should
                      be used
                    type: string
                type: object
            required:
            - instance
            type: object
          status:
            description: PgUserStatus defines the observed state of PgUser
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
